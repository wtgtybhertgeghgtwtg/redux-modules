import {
  createActionCreator,
  createModule,
  createReducer,
  formatType,
} from '../../../redux-modules/src';

export {createActionCreator, createModule, createReducer, formatType};

export default createModule;
